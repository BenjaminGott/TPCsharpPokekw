<UserControl x:Class="TpCsharpPoKEKW.MVVM.View.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TpCsharpPoKEKW.MVVM.View"
             mc:Ignorable="d" 
             
              xmlns:view="clr-namespace:TpCsharpPoKEKW.MVVM.View"
              xmlns:vm="clr-namespace:TpCsharpPoKEKW.MVVM.ViewModel"
             d:DataContext="{d:DesignInstance Type=vm:MainViewVM}"
             
             d:DesignHeight="450" d:DesignWidth="800"
             >
    <Grid>
        <StackPanel VerticalAlignment="Center" >
        <Button Content="Settings" Command="{Binding  HandleRequestSettings}" Width="200"></Button>
            <Button Content="LogIn" Command="{Binding  HandleRequestLogIn}" Width="200" ></Button>
        </StackPanel>
    </Grid>
</UserControl>
